/** Phase 10: financial chart module — data wiring to Alpha hooks lives in TraderViewPanel. */
export default function FCFLineChart(_props: { annual?: unknown[] }) {
  return <div className="co-chart-stub" style={{ minHeight: 140, background: 'rgba(16,20,32,0.5)', borderRadius: 6, border: '1px solid rgba(255,255,255,0.06)' }} />
}
